//Importar a instancia do Express configurada em index.js
const app = require("./index");

//Inicia o servidor na porta 3000, nesta caso a API sera acessivel em http://localhost:5000
app.listen(5000);
