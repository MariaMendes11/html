//Importar a instancia do Express configurada em index.js
const app = require("./index");

//Inicia o servidor na porta 3000, nesta caso a API sera acessivel em http://localhost:3000
app.listen(3000);
